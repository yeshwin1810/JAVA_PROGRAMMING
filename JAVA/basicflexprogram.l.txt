%{
#include <stdio.h>
%}

%%
[0-9]+      { printf("Integer: %s\n", yytext); }
[a-zA-Z]+   { printf("Word: %s\n", yytext); }
.           { printf("Other character: %s\n", yytext); }
%%

int main() {
    yylex();
    return 0;
}
